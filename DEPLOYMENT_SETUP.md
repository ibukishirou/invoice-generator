# GitHub Pages デプロイ設定手順

このリポジトリをGitHub Pagesで公開するには、以下の手順を実行してください。

## 📋 設定手順

### 1. GitHubリポジトリページを開く

https://github.com/ibukishirou/invoice-generator にアクセス

### 2. Settings（設定）タブを開く

リポジトリページの上部メニューから「**Settings**」をクリック

### 3. Pages設定を開く

左側のサイドバーから「**Pages**」をクリック

### 4. デプロイソースを変更

**Source（ソース）**セクションで：
- ドロップダウンメニューから「**GitHub Actions**」を選択
- これにより、`.github/workflows/deploy.yml` ワークフローが使用されます

### 5. 変更を保存

設定は自動的に保存されます。

### 6. ワークフローをトリガー

このファイルと一緒にコミットされた `.github/workflows/deploy.yml` がmainブランチにpushされると、自動的にデプロイが開始されます。

```bash
git push origin main
```

### 7. デプロイ状況を確認

- リポジトリの「**Actions**」タブでワークフローの実行状況を確認できます
- デプロイが完了すると、以下のURLでアクセス可能になります：

**🌐 公開URL:** https://ibukishirou.github.io/invoice-generator/

## ⚠️ 重要な注意事項

### GitHub Actionsの権限設定

もしワークフローの実行時に権限エラーが発生した場合：

1. リポジトリの「**Settings**」→「**Actions**」→「**General**」を開く
2. 「**Workflow permissions**」セクションで以下を設定：
   - ✅ **Read and write permissions** を選択
   - ✅ **Allow GitHub Actions to create and approve pull requests** にチェック
3. 「**Save**」をクリック

### 初回デプロイ後

- 初回デプロイには数分かかる場合があります
- デプロイ完了後、ブラウザのキャッシュをクリアしてページを再読み込みしてください

## 🔄 今後の更新について

mainブランチに変更をpushするたびに、自動的にビルドとデプロイが実行されます。

```bash
git add .
git commit -m "Update feature"
git push origin main
```

## 🐛 トラブルシューティング

### ページが真っ白な場合

1. Actions タブでワークフローが正常に完了しているか確認
2. ブラウザのキャッシュをクリア（Ctrl + Shift + R または Cmd + Shift + R）
3. Settings > Pages で Source が「GitHub Actions」になっているか確認

### ワークフローがエラーになる場合

1. Actions タブでエラーログを確認
2. Workflow permissions の設定を確認
3. `package.json` の依存関係が正しいか確認

---

**参考リンク:**
- [GitHub Pages ドキュメント](https://docs.github.com/pages)
- [GitHub Actions ドキュメント](https://docs.github.com/actions)
